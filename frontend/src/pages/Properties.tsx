import PropertyList from '../components/PropertyList';

const Properties = () => {
  return (
    <div style={{ paddingTop: '40px' }}>
      <PropertyList />
    </div>
  );
};

export default Properties;
